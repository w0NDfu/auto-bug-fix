def plan_task(log: str):
    return {
        "steps": [
            "analyze error",
            "locate code",
            "retrieve context",
            "generate patch",
            "validate and reflect"
        ]
    }